[package]
name = "sdkwork-router-compliance-app-api"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork-appstore-compliance-service = { path = "../sdkwork-appstore-compliance-service" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
