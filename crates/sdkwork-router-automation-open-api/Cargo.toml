[package]
name = "sdkwork-router-automation-open-api"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork-appstore-release-service = { path = "../sdkwork-appstore-release-service" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
