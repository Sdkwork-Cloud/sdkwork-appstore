[package]
name = "sdkwork-router-metrics-backend-api"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork-appstore-catalog-service = { path = "../sdkwork-appstore-catalog-service" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
