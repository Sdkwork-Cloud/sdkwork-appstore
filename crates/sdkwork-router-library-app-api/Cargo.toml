[package]
name = "sdkwork-router-library-app-api"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork-appstore-library-service = { path = "../sdkwork-appstore-library-service" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
