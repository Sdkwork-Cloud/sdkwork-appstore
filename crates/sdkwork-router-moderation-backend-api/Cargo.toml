[package]
name = "sdkwork-router-moderation-backend-api"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork-appstore-moderation-service = { path = "../sdkwork-appstore-moderation-service" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
