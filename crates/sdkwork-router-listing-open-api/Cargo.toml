[package]
name = "sdkwork-router-listing-open-api"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork-appstore-listing-service = { path = "../sdkwork-appstore-listing-service" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
