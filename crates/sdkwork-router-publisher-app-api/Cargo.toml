[package]
name = "sdkwork-router-publisher-app-api"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork-appstore-publisher-service = { path = "../sdkwork-appstore-publisher-service" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
