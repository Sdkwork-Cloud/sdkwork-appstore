# SDKWork App Store Workspace Metadata

Source-controlled agent workspace for `sdkwork-appstore`.

- `skills/`: repository-local skills (placeholder)
- `plugins/`: repository-local plugins (placeholder)

This directory is not runtime state and must not contain secrets or generated SDK transport output.
