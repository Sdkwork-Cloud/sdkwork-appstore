# Appstore Backend SDK Contract

`component.spec.json` is the machine-readable contract for the operator-only Appstore backend SDK
family. The generated transport is owned only by `sdkgen`; app consumers must not import this
family.
