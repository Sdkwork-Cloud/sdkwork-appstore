export interface CatalogFeaturedSlot {
  id: string;
  slotCode: string;
  listingId: string;
  status: string;
  audienceScope: string;
  platformScope: string;
  regionScope: string[];
  startsAt: string;
  endsAt: string;
}
