# Appstore App SDK Contract

`component.spec.json` is the machine-readable contract for the Appstore app SDK family. The
authored authority and derived generator input live under `../openapi/`; transport output is owned
only by `sdkgen` under `../sdkwork-appstore-app-sdk-typescript/generated/server-openapi/`.
