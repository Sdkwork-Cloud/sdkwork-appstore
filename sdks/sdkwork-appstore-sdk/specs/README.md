# Appstore Open SDK Contract

`component.spec.json` is the machine-readable contract for the Appstore open SDK family. Protected
operations use the generated API-key credential surface; this family does not share the app login
TokenManager.
